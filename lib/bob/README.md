# lib/bob
IBM Bob agent integration layer.

NOTE: No client code is written here until we verify that a Bob API
is actually accessible from this project. Do not create speculative
API wrappers or spend Bobcoins unnecessarily.

Will be populated only after confirming the available Bob interface.
